# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main
#       - develop

# jobs:
#   test:
#     runs-on: ubuntu-latest
    
#     strategy:
#       matrix:
#         python-version: ['3.10']
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
      
#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v4
#         with:
#           python-version: ${{ matrix.python-version }}
#           cache: 'pip'
      
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install pytest httpx
      
#       - name: Run pytest tests
#         run: |
#           pytest tests/ -v --tb=short
#         env:
#           # Set environment variables for testing (use defaults from config)
#           GROQ_API_KEY: ${{ secrets.GROQ_API_KEY || '' }}
#           ADMET_AI_URL: https://shdwRow-ailixir-admet.hf.space
#           CHEMICAL_AI_URL: https://RottenShadow-ailixir-chemical-rag.hf.space
#           DRUG_REPURPOSING_URL: https://RottenShadow-ailixir-drug-repurposing.hf.space
#           GENERATION_SERVICE_URL: https://shdwRow-ailixir-generation.hf.space

#   build:
#     runs-on: ubuntu-latest
#     needs: test
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
      
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3
      
#       - name: Build Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: app/Dockerfile
#           push: false
#           tags: scientific-os:latest
      
#       - name: Verify Docker build
#         run: |
#           echo "Docker image built successfully!"
#           docker build -t scientific-os:latest -f app/Dockerfile .
      
#       - name: Run container health check (optional)
#         run: |
#           docker run --rm --entrypoint python scientific-os:latest -c "import app.main; print('✓ Application imports successful')"

#   lint:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
      
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.10'
#           cache: 'pip'
      
#       - name: Install development dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install flake8 black isort
      
#       - name: Check code formatting with black
#         run: black --check app/ tests/ --line-length 120
#         continue-on-error: true
      
#       - name: Sort imports with isort
#         run: isort --check-only app/ tests/
#         continue-on-error: true
      
#       - name: Lint with flake8
#         run: flake8 app/ tests/ --max-line-length=120 --ignore=E501,W503
#         continue-on-error: true
